*.iml
.gradle
/.idea
.DS_Store
**/build
/captures
.externalNativeBuild
.cxx

# local.properties nese API klice (SUPABASE_*, MAL_*, ANILIST_*) - nesmi do repa.
# Vzor pokryva i zalozni varianty (local.properties.bak, .orig, ...): jedna takova
# se uz jednou omylem zacommitovala - nastesti prazdna, takze nic neuniklo.
local.properties*

# Firebase — obsahuje ID projektu, není potřeba (a nechceme) verzovat
app/google-services.json

# Supabase CLI - lokální metadata linknutého projektu, ne zdrojový kód
supabase/.temp

# Interní poznámky pro AI asistenta - nemá se zveřejňovat mezi soubory repa
CLAUDE.md

# Docasne soubory pro dlouhe commit/release zpravy (viz release skripty)
commitmsg.tmp
relnotes.tmp

# Superpowers brainstorming companion - dočasné mockupy, ne zdrojový kód
.superpowers/
